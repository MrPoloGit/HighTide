# cnn Design Decisions

Per-platform notes for the cnn benchmark — a NNgen-generated CNN inference accelerator (Verilog produced by the upstream NNgen Python toolchain), exercising 2rw on-chip SRAMs as activation / weight buffers.

| Variant | Description |
|---|---|
| `cnn` | Single-design with 65 SRAM instances across 4 distinct sizes (16×512, 16×8192, 16×32768, 64×256). |

FakeRAM macros live at `designs/<platform>/cnn/sram/{lef,lib}/`. The behavioural Verilog stubs `fakeram_w*_l*.v` ship with the NNgen sources and stay in the `:rtl` filegroup so yosys can elaborate against them.

## FakeRAM regeneration (2026-05-13)

- **Generator**: `bsg_fakeram` (VLSIDA fork @ `asap7-area-calib-v2`). Run via `tools/regenerate_sram.sh cnn <platform>`.
- **Cfg**: `designs/src/cnn/dev/generated/fakeram_{asap7,nangate45,sky130hd}.cfg` — 4 entries each, `2rw` ports with `no_wmask`.
- **Prior generator**:
  - asap7: OpenFakeRAM (LEFs marked `# Generated by OpenFakeRAM`), committed at `designs/src/cnn/fakeram_*.{lef,lib}` — now deleted, replaced by `designs/asap7/cnn/sram/{lef,lib}/`.
  - nangate45: in-repo `designs/src/cnn/dev/gen_fakeram_nangate45.py` (area_per_bit heuristic) — deleted.
  - sky130hd: in-repo `designs/src/cnn/dev/gen_fakeram_sky130hd.py` (area_per_bit heuristic) — deleted.

### Size deltas (worst macro per platform)

| Platform | Macro | Old (µm²) | New (µm²) | Aspect old → new |
|---|---|---:|---:|---|
| asap7 | `fakeram_w16_l32768` | 19 928 | 309 588 | 1.77:1 → 1.25:1 |
| nangate45 | `fakeram_w16_l32768` | 1 992 926 | 1 614 255 | 2.0:1 → 1.20:1 |
| sky130hd | `fakeram_w16_l32768` | 2 100 491 | 19 770 053 | 2.05:1 → 1.23:1 |

sky130hd grew ~10× because:
1. The previous python-script estimate (2.1 mm²) was unrealistically dense for 130nm — real OpenRAM-style 6T bitcells are ~1.07 × 1.74 µm = 1.86 µm², which on a 524 Kb 2rw memory gives ~20 mm² after periphery + dual-port overhead. The new size matches OpenRAM 130nm density.
2. The `MACRO_PLACEMENT_TCL` files (asap7, nangate45, sky130hd) hand-placed all 65 instances assuming the old footprints — they're dropped, RTLMP places.
3. sky130hd's die had to grow from 7 168 × 7 000 µm (50 mm²) to 12 000 × 12 000 µm (144 mm²) to fit the 4 × 20 mm² 32K macros plus the rest. nangate45 + asap7 die areas unchanged.

### Why analytical (not CACTI) on sky130hd

CACTI's optimizer minimises cycle time for an L3-cache-like target; with cnn's 16-bit-wide × 32K-deep memory and sky130hd's bitcell geometry, that lands at column-mux=1 and a 21 mm × 0.7 mm macro (30:1 aspect) — usable for area-estimate cells in a research context, not a real floorplan. The bsg_fakeram fork's analytical formula uses dynamic column muxing to land near 1.5:1 aspect and matches the same overhead model used for asap7.

nangate45 stays on CACTI; its aspects come out 1.2–1.6 for cnn's four sizes.
